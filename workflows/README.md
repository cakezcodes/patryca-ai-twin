# Workflows

ComfyUI workflow JSON files will live here once the actual workflows are built and tested.

## Planned workflows

- `identity-test.json` — basic Person LoRA identity test
- `reference-generation.json` — Person LoRA + reference conditioning
- `portrait.json` — controlled portrait validation
- `full-body.json` — body/identity validation
- `upscale.json` — final detail/upscale pass

Do not commit embedded private images or model weights inside workflow files. Keep paths/configuration portable where practical.
